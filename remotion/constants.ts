// Defaults used at composition registration time. The actual render
// dimensions/duration come from inputProps via calculateMetadata, but the
// Composition definition still needs starting values.

export const COMP_NAME = 'Editor'

export const DEFAULT_FPS = 30
export const DEFAULT_WIDTH = 1080
export const DEFAULT_HEIGHT = 1920
export const DEFAULT_DURATION_IN_FRAMES = 30 // 1s placeholder
